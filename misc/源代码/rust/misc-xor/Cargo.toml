[package]
name = "misc-xor"
version = "0.1.0"
edition = "2021"
description = "XOR cipher analysis and decryption"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "2"

[profile.release]
opt-level = 3
lto = true
strip = true
