[package]
name = "misc-entropy"
version = "0.1.0"
edition = "2021"
description = "Entropy visualization - generate PNG heatmap"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "2"

[profile.release]
opt-level = 3
lto = true
strip = true
