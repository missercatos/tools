[package]
name = "misc-analyze"
version = "0.1.0"
edition = "2021"
description = "File analysis: magic, entropy, metadata, hidden data detection"

[dependencies]
clap = { version = "4", features = ["derive"] }
colored = "2"

[profile.release]
opt-level = 3
lto = true
strip = true
