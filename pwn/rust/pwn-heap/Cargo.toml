[package]
name = "pwn-heap"
version = "0.1.0"
edition = "2021"
description = "堆元数据解析 (PWN tool)"

[dependencies]
goblin = "0.8"
clap = { version = "4", features = ["derive"] }
colored = "2"

[profile.release]
opt-level = 3
lto = true
strip = true
